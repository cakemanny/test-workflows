on:
  push: {}

jobs:
  try-action:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        a: [1, 2]
    steps:
      - uses: actions/checkout@v5
      - name: Run Test Action
        uses: ./test-action
        with:
          a: ${{ matrix.a }}
